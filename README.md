##Prerequisites
* A backup of the pineapp database
* The source code (including settings.py) for the Django site
* Adjust the makefile setup targets, `init_django` and `copy_db` to reflect the above

##Build 

`make init` or `make`

##Run

`make start`

